/**
 * @file 类型定义 - 兼容再导出
 * @description 实体与领域常量已上移 @celery/core（3.0 阶段2）。
 *              本文件保留为 `@/types` 的兼容入口，应用内既有 import 无需改动；
 *              新代码应直接从 `@celery/core` 导入。
 */

export * from '@celery/core';
