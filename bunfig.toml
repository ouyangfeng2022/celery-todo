# 固定使用 hoisted（扁平）node_modules 布局。
#
# 历史：Bun 1.3 起 isolated 布局成为默认，曾导致（已移除的 2.x Electron 壳）
# electron-builder 漏打传递依赖。2.x 已于 3.4.2 之后退役，此处保留 hoisted
# 是为了避免整套 workspace 重新洗牌 node_modules —— 当前布局经长期验证。
[install]
linker = "hoisted"
