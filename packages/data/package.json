{
  "name": "@celery/data",
  "private": true,
  "version": "2.20.0",
  "description": "Repository 契约、查询 DTO、v3 导入导出格式与内存适配器（Tauri / Expo / 测试共用）",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./generated/*": "./src/generated/*"
  },
  "scripts": {
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:run": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.14.10",
    "typescript": "^5.5.3",
    "vitest": "^2.0.2"
  }
}
